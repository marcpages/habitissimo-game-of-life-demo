# Conway's Game of Life demo for Habitissimo

To run the game execute the following command on terminal:
 
`php cgol-cli.php`

### Run tests

`./vendor/bin/phpunit`

### Run dockerized

`docker-compose up -d`
